#include "main.h"
/**
 * mul - multiplies two numbers
 *
 * @a: first number
 *
 * @b: second number
 *
 * Return: result (the multiplies of two integers)
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
