#include "main.h"
/**
 * _isupper - checks if the character is uppercase
 * @c: The character to check
 *
 * Return: On success 1.
 */

int _isupper(int c)
{
	int i;

	for (i = 65; i <= 90; i++)
	{
		if (c = _putchar(i))
		{
			return (1);
		} else 
		{
			return (0);
		}
	}
}
